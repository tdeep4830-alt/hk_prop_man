[pytest]
asyncio_mode = auto
testpaths = tests

markers =
    integration: marks tests that require a real PostgreSQL + pgvector instance (deselect with '-m "not integration"')
    eval: marks RAGAS evaluation tests that require LLM API keys (only run in CD eval stage)

# Default timeout for unit tests (seconds)
timeout = 30
